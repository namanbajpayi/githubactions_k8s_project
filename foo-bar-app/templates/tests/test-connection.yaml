apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo-bar-app.fullname" . }}-test-connection"
  labels:
    {{- include "foo-bar-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foo-bar-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
