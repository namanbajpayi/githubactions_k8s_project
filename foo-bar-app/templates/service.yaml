apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo-bar-app.fullname" . }}
  labels:
    {{- include "foo-bar-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "foo-bar-app.selectorLabels" . | nindent 4 }}